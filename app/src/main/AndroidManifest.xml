<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.param.recruit">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".EmployerApplicationDisplay"></activity>
        <activity android:name=".EmployerApplications" />
        <activity android:name=".EmployerPostJob" />
        <activity android:name=".EmployerRegister" />
        <activity android:name=".EmployerProfile" />
        <activity android:name=".EmployerLogin" />
        <activity android:name=".EmployeeApplicationDisplay" />
        <activity android:name=".EmployeeApplications" />
        <activity android:name=".EmployeeJobDisplay" />
        <activity android:name=".EmployeeSearch" />
        <activity android:name=".EmployeeProfile" />
        <activity android:name=".SelectUser">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EmployeeLogin" />
        <activity android:name=".EmployeeRegister" />
    </application>

</manifest>